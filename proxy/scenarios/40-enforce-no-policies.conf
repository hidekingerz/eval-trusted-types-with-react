# Enforce sinks and forbid creating any policy (trusted-types 'none'). Nothing string-based can reach a sink.
<LocationMatch "^/[^/]+/enforce-no-policies/">
    Header always set Content-Security-Policy "require-trusted-types-for 'script'; trusted-types 'none'; report-uri /api/csp-report"
</LocationMatch>
