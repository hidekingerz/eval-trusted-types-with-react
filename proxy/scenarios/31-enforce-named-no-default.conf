# Enforce sinks; only 'react-eval' may be created. Creating a default policy is a violation.
<LocationMatch "^/[^/]+/enforce-named-no-default/">
    Header always set Content-Security-Policy "require-trusted-types-for 'script'; trusted-types react-eval; report-uri /api/csp-report"
</LocationMatch>
