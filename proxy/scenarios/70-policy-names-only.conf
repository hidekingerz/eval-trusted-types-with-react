# Only the trusted-types directive (policy-name allow-list) without require-trusted-types-for. Sinks still accept strings.
<LocationMatch "^/[^/]+/policy-names-only/">
    Header always set Content-Security-Policy "trusted-types react-eval default; report-uri /api/csp-report"
</LocationMatch>
