# Enforce sinks only. Any policy name may be created (no trusted-types directive).
<LocationMatch "^/[^/]+/enforce/">
    Header always set Content-Security-Policy "require-trusted-types-for 'script'; report-uri /api/csp-report"
</LocationMatch>
