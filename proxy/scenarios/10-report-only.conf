# Report-Only enforcement: violations are reported but nothing is blocked.
<LocationMatch "^/[^/]+/report-only/">
    Header always set Content-Security-Policy-Report-Only "require-trusted-types-for 'script'; report-uri /api/csp-report"
</LocationMatch>
